name: Lion King Strategy Daily Run

on:
  schedule:
    # 台灣時間早上 08:30 自動執行 (UTC 00:30)
    - cron: '30 0 * * *'
  workflow_dispatch: # 允許手動執行

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install yfinance pandas pandas_ta

      - name: Run Lion Bot Strategy
        run: python lion_bot.py

      - name: Commit and Push Data (Auto Save)
        run: |
          git config --global user.name "LionBot"
          git config --global user.email "bot@lion.strategy"
          # 存檔關鍵：同時儲存 網頁(index.html) 和 帳本(Lion_Ledger.csv)
          git add index.html Lion_Ledger.csv
          
          # 先拉取最新版避免衝突，再提交
          git pull --rebase
          git commit -m "📊 Daily Update: Ledger & Report" || echo "No changes to commit"
          git push
